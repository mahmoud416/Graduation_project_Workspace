# @react-three/drei@10 declares a peer on @react-three/fiber@9, but this app
# pins fiber@8. The combination works at runtime, so relax strict peer
# resolution for npm ci / npm install (CI + Docker honor this automatically).
legacy-peer-deps=true
